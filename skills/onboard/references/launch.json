{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "api",
      "runtimeExecutable": "nix",
      "runtimeArgs": ["develop", "--command", "bash", "-c", "cd api && php artisan optimize:clear && php artisan optimize && php artisan serve --port=9191"],
      "port": 9191
    },
    {
      "name": "api-queue",
      "runtimeExecutable": "nix",
      "runtimeArgs": ["develop", "--command", "bash", "-c", "cd api && php artisan queue:work database --timeout=3000 --tries=5 --queue=high,low,default,subscriptions"]
    },
    {
      "name": "web",
      "runtimeExecutable": "nix",
      "runtimeArgs": ["develop", "--command", "bash", "-c", "cd web && bun run dev"],
      "port": 3000
    },
    {
      "name": "ai-service",
      "runtimeExecutable": "nix",
      "runtimeArgs": ["develop", "--command", "bash", "-c", "cd ai-service && source .venv/bin/activate && fastapi dev"],
      "port": 8000
    },
    {
      "name": "data-service",
      "runtimeExecutable": "nix",
      "runtimeArgs": ["develop", "--command", "bash", "-c", "cd data-service && source .venv/bin/activate && uvicorn app.main:app --reload --port 9999"],
      "port": 9999
    },
    {
      "name": "mailhog",
      "runtimeExecutable": "nix",
      "runtimeArgs": ["develop", "--command", "mailhog"],
      "port": 8025
    },
    {
      "name": "prefect-server",
      "runtimeExecutable": "nix",
      "runtimeArgs": ["develop", "--command", "bash", "-c", "cd orchestration && PREFECT_PROFILE=local .venv/bin/prefect server start"],
      "port": 4200
    }
  ],
  "$comment_prefect_server": "prefect-server is OPT-IN and is deliberately NOT started by run-all.sh. Orchestration has no long-running server of its own; this entry exists only for local flow development (onboard Step 7.5). It binds :4200 — the single port orchestration can contend for. Omit this configuration entirely if Orchestration was not selected. The PREFECT_PROFILE=local prefix is required: without it the server inherits whatever profile is globally active, which may be frndos_prefect (staging AND production)."
}
