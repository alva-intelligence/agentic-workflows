# frndOS — Agent Instructions

> **Version:** {{VERSION}}
> **Generated:** {{DATE}}
> **Do not edit manually** — regenerated by `generate-agents.sh`

> **FOR LLM AGENTS:** After bootstrap, you MUST read this entire file and follow the Session Start Protocol below. If no service directories exist (api/, web/, etc.), this is a fresh workspace — use your ask tool to ask the user: **"Would you like to start onboarding now to set up your development environment?"** If yes, read `.agents/skills/onboard/SKILL.md` and execute it directly (no need to restart for the slash command). If no, tell the user they can run `/onboard` in a new session later. If services exist but no `.workflow-state.json`, welcome the user and explain `/workflow start <feature-slug>`.

---

{{FRAGMENT:session-protocol.core.md}}

---

{{FRAGMENT:workflow-rules.core.md}}

---

{{FRAGMENT:git-conventions.md}}

---

{{FRAGMENT:service-registry.core.md}}

---

## Skills Index (load-on-demand details)

When a command/phase is invoked, load the matching skill's `SKILL.md`. Deep references live alongside as `references/*.md` and should be read when the skill needs them.

| Skill | Use when | Deep references |
|-------|---------|-----------------|
| `onboard` | Fresh workspace setup, `/onboard`, verifying service env | `references/service-registry.md`, `references/external-steps.md`, `references/mcp-configs.md` |
| `setup-workspace` | Bootstrapping the agentic-workflows install itself | — |
| `workflow` | Phase transitions, `/workflow *`, implementation tracking | `references/track-conventions.md`, `references/testing-policy.md`, `references/session-checks.md`, `references/agent-teams.md` |
| `prd` | `prd_creation` phase — authoring the main PRD | `references/conventions.md` |
| `prd-split` | `prd_splitting` phase — creating the feature branch and splitting main PRD into per-service PRDs | `references/conventions.md` |
| `brainstorm` | `brainstorming` phase — multi-choice questioning grounded in service state | `references/question-patterns.md` |
| `lark-sync` | Lark CLI auth, `/lark-sync link`, wiki/task sync | `references/session-check.md` |
| `jj-workflow` | Parallel features via JJ workspaces | `references/rules.md` |
| `workflow-update` | Updating the agentic-workflows instructions themselves | — |
| `lark-task` (symlink) | Lark task CRUD primitives used by `lark-sync` | `references/*` (many) |

**Rules when invoking a skill:**
1. Read the skill's `SKILL.md` fully on invocation.
2. Read the relevant `references/*.md` if the skill points to it or the task needs that depth.
3. Do NOT inline references here — keep this file thin.
