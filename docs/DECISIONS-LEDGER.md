# Decisions ledger — agentic-workflows

Append-only. Newest last. One row per **direction change** — a decision that reverses, retires or
re-scopes something already written down. Routine additions do not belong here.

Obligations, both in the **same commit** as the change:

1. **Banner the corpse.** The superseded passage keeps its text and gains, at its head:
   `> ⛔ **SUPERSEDED** by <label> (YYYY-MM-DD). Kept for history — do not build from this.`
   A superseded spec that still reads as live is a trap for the next session.
2. **Close the question.** If a doc asked someone to approve something, the answer is written next
   to it — including `overtaken by events, never approved` when that is the truth.

| Date | Label | What changed | Supersedes | Why | Cost |
|---|---|---|---|---|---|
| 2026-09-07 | `ONB-MARTS-LOCAL-1` | 7.4b's marts lineage moves from `FRND_ENVIRONMENT=staging` + raw `alembic stamp head` / `upgrade head` to `scripts/db-sync.sh` (`--status` / `--verify` agent-runnable, `--local` user-run). | `skills/onboard/SKILL.md` §7.4b — the "Keep `staging` here" rationale blockquote, now bannered in place. | `staging` resolved the `clickhouse-host-staging` block, which 7.5.3 never created, so the step could not succeed as written; and where that block held a real host, `upgrade head` would have migrated **staging**. `stamp head` on a cluster missing a revision marks it applied and its DDL never runs — silently, forever; frnd-orchestration Do-Not-Touch 4 now forbids it. `local` (FR-25, frnd-orchestration#77) resolves `clickhouse-host` and is checked against the **resolved** host. | `--local` drives Alembic, which loads Prefect Secret blocks, so the marts **write** step is now gated on 7.5.2 + 7.5.3 and 7.4b is split across the ordering gate. Mitigated with a forward gate in 7.4b and a back-pointer at the end of 7.5.3, not by renumbering. |
| 2026-09-07 | `ONB-BLOCKS-SIX-1` | 7.5.3 mandates **six** Secret blocks; `clickhouse-host-staging` = `localhost` is promoted from an optional aside to a required seatbelt. | `skills/onboard/SKILL.md` §7.5.3 — "five Secret blocks" heading, count in the 7.5 overview, `block ls` expectations, and the Step 11 troubleshooting table. | A stray staging signal on a pure-local estate would otherwise resolve a block that does not exist, or worse a real one. The block is a seatbelt: nothing in onboarding selects it. | One more manual block for every new developer, in a step the agent cannot perform (`prefect block create` is denied — UI or nothing). |
| 2026-09-07 | `ONB-LINEAGE-CONTRACT-1` | The two Alembic lineages are documented as having **different** environment contracts. `FRND_ENVIRONMENT` is scoped to data-pipeline; data-service is documented on `CH_MIGRATE_HOST` → `CH_HOST` via `.env`/pydantic-settings, with `_assert_intentional_target` + `FRND_MIGRATE_ALLOW_REMOTE=1`. | `skills/onboard/SKILL.md` §7.4b — "**Carry the prefix on every single Alembic command**", which sat under a heading covering both lineages. | The prefix is inert on data-service: it selects nothing and protects nothing. The two guards also have opposite polarity — data-pipeline fails **unsafe** when unset (→ production), data-service fails **safe** (refuses non-local). Stating one rule for both taught the wrong mental model for whichever lineage the developer touched second. | Documents an **unfixed asymmetry**: data-service has no `db-sync.sh` equivalent, so `stamp head` remains its documented remedy and the silent-skip risk is live on that lineage. Recorded, not solved. |
| 2026-09-07 | `ONB-RUNALL-PORTS-1` | `run-all.sh` splits its port list into `OWNED_PORTS` (9191 3000 8000 9999 1025 8025) and `SHARED_PORTS` (8123 4200). Every kill loop iterates `OWNED_PORTS`; shared ports are reported, never killed. | `skills/onboard/references/run-all-template.sh` — `stop_all()`'s orphan sweep, `kill_ports()`, `check_ports()`, the usage text, and the header claim that the script "never starts or stops them". | The header was false on three paths at once: `--stop` `kill -9`'d 8123/4200, `--kill-ports` killed them while advertising only four ports, and the conflict warning **recommended** `--kill-ports`. All three destroyed the local ClickHouse and Prefect the same onboarding run had just told the developer to leave running. | Workspaces generated before this date still carry the old script. Step 12 now tells the reader to grep for `8123` in a kill loop before running `--stop`, rather than silently assuming the fix is present. |

## Open questions

| Asked in | Question | Status |
|---|---|---|
| `ONB-LINEAGE-CONTRACT-1` | Should data-service get a verified-stamp tool of its own, so `stamp head` stops being its documented remedy? `db-sync.sh` cannot be reused — it is hardcoded to `frnd_agg_marts`. | **Open.** Needs a decision from Fahmi; it is a new script in `frnd-clickhouse-api`, not a doc edit. |
| `ONB-MARTS-LOCAL-1` | `setup-local-demo.sh`'s `apply_dir` globs `*.sql` and runs `017_fix_social_mart_sorting_keys.sql` (`DO NOT RUN THIS AUTOMATICALLY`) unattended on every laptop it provisions. | **Open, deliberately not fixed.** Fixing it changes what the script applies. Documented in 7.4c so no doc edit implies it is handled. |
